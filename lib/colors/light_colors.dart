import 'dart:ui';

class LightTunerColors {
  static const Color gray = Color(0xFF555555); // Used for settings text.
  static const Color gray60 = Color(0x99D8D8D8); // Lighter gray.
  static const Color buttonText = Color(0xFFFFFFFF); // Used by "Ready" as the text color.
  static const Color primaryBackground = Color(0xFFD8D8D8); // Background and primary color.
  static const Color inputBackground = Color(0xFFAAAAAA); // Used for sliders' inactive color, input backgrounds, etc.
  static const Color cardBackground = Color(0X50FEFEFE); // Used for sliders.
  static const Color buttonColor = Color(0xFF454545); // Used by "Ready" as the background.
  static const Color focusColor = Color(0xFF393939); // Bottom navigation bar icons
}
